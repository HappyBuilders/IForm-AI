# 金额\数值

## 明细子表中的计算金额\数值控件运算值，并将结果显示在主表控件中

1. 勾选明细子表上的金额\数值控件属性的"列运算"，金额和数值控件的特有属性，控件放到明细子表中显示。用于计算数值/金额列的合计、最大值、最小值、平均数或行数统计。

2. 设置主表控件，因为明细子表的列运算的结果是一个计算值，在表单上不会单独保存，但当有在主表上显示子表的列合计的需求时，需要将这个合计值传到主表某个数值/金额字段上时，需要选择主表字段。待选值为主表上没有定义公式的数值/金额控件。